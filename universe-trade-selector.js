/* A Bulls App — Trade Mission Selector v0.1 */
(()=>{'use strict';const T=()=>window.UniverseTradeMission,U=()=>window.UniverseFlight;function fmtTime(t){try{return new Date(Number(t)*1000).toLocaleString()}catch{return String(t||'')}}function fmtPnl(v){if(v==null||!Number.isFinite(Number(v)))return'';const n=Number(v);return `${n>=0?'+':'-'}$${Math.abs(n).toLocaleString(undefined,{maximumFractionDigits:2})}`}function mount(){const style=document.createElement('style');style.textContent=`#tradeSelector{position:fixed;inset:10% 6%;z-index:55;padding:14px;border:1px solid #63ffc544;background:#020a08f5;backdrop-filter:blur(22px);border-radius:18px;color:#eafff7;overflow:auto;font:10px Inter,system-ui}#tradeSelector[hidden]{display:none}#tradeSelector .head{display:flex;justify-content:space-between;align-items:center;position:sticky;top:-14px;background:#020a08f5;padding:10px 0;z-index:2}#tradeSelector h2{margin:0;font-size:15px;letter-spacing:.08em}#tradeList{display:grid;gap:8px}#tradeList article{border:1px solid #ffffff18;background:#07110e;border-radius:12px;padding:11px}#tradeList article.buy{border-color:#63ffc544}#tradeList article.sell{border-color:#ff536f44}#tradeList .meta{opacity:.72;line-height:1.5;margin:5px 0 9px}#tradeList button,#tradeSelector .close{border:1px solid #63ffc555;background:#0c241b;color:#eafff7;border-radius:999px;padding:8px 11px;font:800 9px Inter,system-ui}#tradeMissionButton{position:fixed;right:14px;top:166px;z-index:46;border:1px solid #ffd36a55;background:#1a1206e8;color:#ffe49a;border-radius:999px;padding:9px 11px;font:800 9px Inter,system-ui}#tradeMissionButton[hidden]{display:none}`;document.head.appendChild(style);const launch=document.createElement('button');launch.id='tradeMissionButton';launch.textContent='✦ PLAY A TRADE';launch.hidden=true;document.body.appendChild(launch);const panel=document.createElement('section');panel.id='tradeSelector';panel.hidden=true;panel.innerHTML='<div class="head"><div><h2>HISTORICAL TRADE MISSIONS</h2><div id="tradeCount" style="opacity:.6;margin-top:3px"></div></div><button class="close" id="tradeClose">×</button></div><div id="tradeList"></div>';document.body.appendChild(panel);function render(){const rows=T()?.listLoaded?.()||[];launch.hidden=!rows.length;document.getElementById('tradeCount').textContent=rows.length?`${rows.length} indexed trade${rows.length===1?'':'s'} available`:'No indexed trades in the loaded field';const list=document.getElementById('tradeList');list.innerHTML='';for(const tr of rows){const a=document.createElement('article');a.className=tr.side==='sell'?'sell':'buy';const pnl=fmtPnl(tr.pnlUsd);a.innerHTML=`<b>${String(tr.side||'TRADE').toUpperCase()}</b>${pnl?` · <strong style="color:${Number(tr.pnlUsd)>=0?'#63ffc5':'#ff536f'}">${pnl}</strong>`:''}<div class="meta">${fmtTime(tr.time)}<br>Magnitude ${Number(tr.magnitude||0).toFixed(2)}${tr.category?` · ${tr.category}`:''}<br>${String(tr.id||'').slice(0,34)}</div><button>PLAY THIS TRADE</button>`;a.querySelector('button').onclick=()=>{T()?.buildTrade?.(tr);panel.hidden=true;if(window.UniverseWarp&&U()?.state?.destination)setTimeout(()=>{try{window.UniverseWarp.jump()}catch{}},160)};list.appendChild(a)}}launch.onclick=()=>{render();panel.hidden=false};document.getElementById('tradeClose').onclick=()=>panel.hidden=true;addEventListener('universeflight:trade-mission-candidates',render);addEventListener('universeflight:sector',()=>setTimeout(render,80));addEventListener('universeflight:experience',e=>{if(e.detail.mode!=='mission')panel.hidden=true});setTimeout(render,800)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount()})();