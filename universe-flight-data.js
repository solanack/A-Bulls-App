/* A Bulls App — Universe Flight data bridge
 * Fetches only bounded, cacheable Intelligence Universe snapshots/evidence.
 * No per-frame network traffic. Falls back to fog/local destination when unavailable.
 */
(()=>{'use strict';
const s=v=>String(v??'').trim(),n=v=>Number.isFinite(Number(v))?Number(v):0,clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const BASE58=/^[1-9A-HJ-NP-Za-km-z]{32,44}$/;
const state={apiBase:'',universe:'solana',last:null,loading:false,error:null};
function hash(v){let h=2166136261;for(const c of s(v)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0}function rng(seed){return()=>((seed=Math.imul(seed^seed>>>15,1|seed)+0x6D2B79F5|0)^seed>>>7)>>>0)/4294967296}
function endpoint(path,params={}){const u=new URL(state.apiBase+path,location.origin);for(const[k,v]of Object.entries(params))if(v!==''&&v!=null)u.searchParams.set(k,String(v));return u.toString()}
async function get(path,params){const r=await fetch(endpoint(path,params),{headers:{accept:'application/json'}});if(!r.ok)throw new Error(`intelligence_http_${r.status}`);return r.json()}
function projectionRows(payload){const snap=payload?.snapshot||payload?.data?.snapshot||payload?.data||payload;for(const k of['projection','events','items','entities','particles'])if(Array.isArray(snap?.[k]))return snap[k];if(Array.isArray(snap))return snap;return[]}
function toSector(rows,seed,coverage){const R=rng(hash(seed)),radius=1800,seen=new Set(),particles=[],obstacles=[];let magnitude=0,swaps=0,buys=0,sells=0;for(const row of rows.slice(0,1000)){const id=s(row.entityId||row.entity_id||row.id||row.signature);if(!id||seen.has(id))continue;seen.add(id);const kind=s(row.entityKind||row.entity_kind||row.kind||'unknown'),cat=s(row.category||row.eventClass||row.event_class),mag=clamp(n(row.magnitudeBand||row.magnitude_band)||.12,.05,1),a=R()*Math.PI*2,d=Math.sqrt(R())*radius;const p={id,entityKind:kind,category:cat,x:Math.cos(a)*d,y:Math.sin(a)*d,r:2+mag*7,k:R(),magnitude:mag,evidence:row.evidence||null};particles.push(p);magnitude+=mag;if(cat==='swap'){swaps++;const side=s(row.side);if(side==='buy')buys++;if(side==='sell')sells++;}if(kind==='transaction'||cat==='program')obstacles.push({id:`e:${id}`,x:p.x,y:p.y,r:15+mag*30,hp:1+Math.floor(mag*3),evidence:p.evidence});}return{seed,particles,obstacles:obstacles.slice(0,120),market:{volume:clamp(particles.length/300,0,1),volatility:clamp(magnitude/Math.max(1,particles.length),0,1),buyPressure:swaps?buys/Math.max(1,buys+sells):.5},coverage};}
async function loadUniverse(universe='solana'){state.loading=true;state.error=null;try{const data=await get('/api/intelligence/universe-snapshot',{universe,window:300,limit:900}),rows=projectionRows(data),sector=toSector(rows,`universe:${universe}`,data?.snapshot?.coverage||data?.coverage||null);state.universe=universe;state.last={kind:'universe',data,sector};window.UniverseFlight?.setSector(sector);return state.last;}catch(e){state.error=s(e.message||e);throw e;}finally{state.loading=false}}
async function travel(value,type='auto'){const id=s(value);if(!id)throw new Error('destination_required');window.UniverseFlight?.setDestination(id,type);state.loading=true;state.error=null;try{const q={universe:state.universe,from:Math.floor(Date.now()/1000)-86400,to:Math.floor(Date.now()/1000),limit:900};if(type==='wallet'||(type==='auto'&&BASE58.test(id)))q.wallet=id;else if(type==='token'||type==='nft')q.mint=id;const data=await get('/api/intelligence/universe-evidence',q),rows=Array.isArray(data?.events)?data.events:[],sector=toSector(rows,`${type}:${id}`,data?.coverage||null);if(rows.length)window.UniverseFlight?.setSector(sector);state.last={kind:'destination',id,type,indexed:rows.length>0,data,sector};return state.last;}catch(e){state.error=s(e.message||e);state.last={kind:'destination',id,type,indexed:false,error:state.error};return state.last;}finally{state.loading=false}}
window.UniverseFlightData={state,loadUniverse,travel,setApiBase:v=>state.apiBase=s(v).replace(/\/$/,'')};
})();