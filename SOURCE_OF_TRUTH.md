# A Bulls App — source of truth

QUERY and the Field are one living organism.

- Field particles swarm into the Grey head and keep breathing.
- QUERY does not open a second screen.
- The only extra UI is a small slot under the head.
- ASK stays on the Field and reads the live Worker: `/api/intelligence/resolve?query=`
- The dense 3D template still lives on `feature/query-isolated-organism-v2` as `js/quantum/alien-head-template.mjs` (470KB). Copy that file onto this branch in GitHub to finish the real head.
- Do not deploy this branch to abullsapp.com until the Cloudflare preview looks right.
