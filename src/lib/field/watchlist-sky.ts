import type { FieldParticle, UniverseSnapshot } from "./types";
import type { WatchItem } from "./watchlist";
import { canonicalUniverseId } from "./galaxies";
import { particleMint } from "./volume-sky";
import { mintKey } from "./watchlist";
function position(index:number,kind:WatchItem["subjectKind"]):[number,number,number]{const group=index%40,angle=group*2.399963229728653;if(kind==="token"){const radius=18+Math.sqrt(group+1)*7;return[Math.cos(angle)*radius,-18+(index%4)*5,Math.sin(angle)*radius];}const radius=24+Math.sqrt(group+1)*8;return[Math.cos(angle)*radius,18+(index%7)*6,Math.sin(angle)*radius];}
function findExisting(base:UniverseSnapshot|null,item:WatchItem){if(!base)return null;const key=mintKey(item.subjectId);return base.particles.find(p=>item.subjectKind==="token"?mintKey(particleMint(p)??"")===key:mintKey(typeof p.metadata?.wallet==="string"?p.metadata.wallet:"")===key)??null;}
export function buildWatchlistSnapshot(items:readonly WatchItem[],base:UniverseSnapshot|null):UniverseSnapshot{const now=Date.now(),particles:FieldParticle[]=items.slice(0,100).map((item,index)=>{const existing=findExisting(base,item),cosmicKind=item.subjectKind==="token"?"planet" as const:"star" as const,seed:FieldParticle=existing??{id:canonicalUniverseId(cosmicKind,item.subjectId,item.galaxyId),kind:item.subjectKind==="token"?"token":"wallet",cosmicKind,originGalaxyId:item.galaxyId,verificationState:"saved-reference",observedAt:item.addedAt,category:item.subjectKind==="token"?"swap":"transfer",magnitudeBand:item.subjectKind==="token"?.62:.54,position:[0,0,0],source:"device-watchlist",metadata:item.subjectKind==="token"?{mint:item.subjectId,symbol:item.symbol,name:item.name}:{wallet:item.subjectId,name:item.name||`WALLET ${item.subjectId.slice(0,6)}…`}};return{...seed,cosmicKind,position:position(index,item.subjectKind),metadata:{...(seed.metadata??{}),name:item.subjectKind==="wallet"?(item.name||`WALLET ${item.subjectId.slice(0,6)}…`):(seed.metadata?.name??item.name),skyRole:"watch",watchReference:true,watchSubjectKind:item.subjectKind,watchSubjectId:item.subjectId,watchSavedAt:item.addedAt}};});return{galaxyId:"galaxy-zero",windowStart:particles.length?Math.min(...particles.map(p=>p.observedAt)):now,windowEnd:now,observedEventCount:0,samplingPolicy:"personal watchlist references; local-first; no provider fetch",coverageStatement:particles.length?"Saved token planets sit below saved wallet stars. A saved reference is not a live market observation unless indexed evidence is already attached.":"Your watchlist is empty. No token planet or wallet star was invented.",sources:["device-watchlist",...(base?.sources??[])],particles};}
