A BULLS APP
BUILD: 7.0.1-upgrade
RELEASE: BULL INVADERS PERFORMANCE · FLAT-PAGES COMPATIBILITY · PILOT SYSTEM REMOVED

Expected backend:
WORKER 8.0.2

Upgrade branch:
upgrade/bull-invaders-performance-v1

Changes in this branch:
- Bull Invaders hitch-aware local performance diagnostics and adaptive decorative-FX pressure handling.
- Resilient PWA precaching and stale-while-revalidate static asset delivery.
- Cloudflare Pages rewrites make the flattened GitHub artifact resolve the existing js/, css/, vendor/, and assets/ URLs.
- Abandoned Pilot presentation and Pilot art manifest removed; Pilot assets are no longer precached.
- Bull Pen compatibility remains hidden behind the existing feature flag.

Ranked scoring, hitboxes, projectile rules, read-only wallet constraints, and payment constraints remain unchanged.
