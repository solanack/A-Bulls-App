# A Bulls App repair review — 2026-09-08

## Baseline and product direction

Reviewed against GitHub main `3c630af85c45e301c3180bfaf8f25858999619bd`; all 287 tracked files were recovered and matched against GitHub blob hashes before changes. The existing minimal interface and native Three.js/WebGL renderer remain the foundation.

The product direction is a navigable Galaxy Zero containing protocol galaxies, each leading to real tokens, useful trader intelligence, then deeper evidence and replay. Decorative particles must not be counted as live tokens or certified as market evidence. Read-only interaction remains enforced. Bull Invaders, LIFE, wallet execution, and trading are not restored.

## Repairs

- Corrected undefined reversed-edge GLSL smoothstep calls in field and alien shaders; aligned touch picking with Three.js world transforms and focus positioning; removed zero-morph position distortion.
- Preserved PONS flattening during animation, reset camera framing on galaxy changes, and paused rendering while the page is hidden.
- Prevented late hydration/query completions from restoring an abandoned view; protected browser storage reads/writes and restored Solana watchlist persistence and case-sensitive identities.
- Normalized indexed timestamps to milliseconds. Corrected live-star counting and prioritized actual tokens over decorative particles within the rendering budget.
- Added bounded, cached DexScreener batch enrichment through the intelligence Worker for up to 30 indexed Solana/Pump token addresses per request. It does not increase Helius membership or spend Helius RPC credits.
- Kept PONS market-cap ranking separate from Solana five-minute admission requirements. Pinned PONS lookup is not claimed as verified factory membership; stale ranked observations are flagged.
- Corrected PONS discovery to the documented Trading.Trades shape; selected latest token observations before applying market-cap gates; allowed stronger ranked candidates to enter after the stability window.
- Added bounded PONS cursor bootstrapping and multi-chunk progress; malformed log responses cannot delete launches; scanning no longer activates unranked membership when ranking is enabled. Scheduled ranking follows indexing.
- Added provider timeouts/failover, preserved partial market evidence, and stopped converting null metrics into zero.
- Trader speech now prioritizes valuation, five-minute activity (one-hour when missing), buy/sell counts, liquidity, concentration, and launchpad/authority observations. Current lookup failures use a brief actionable message.
- Unified test coverage and deployment commands. Both Workers must pass checks before publishing. Missing Cloudflare credentials cause an explicit failure. A public release marker identifies the frontend commit actually served.

## Validation and limits

- Full repository `npm test` passed, including new behavioral regressions. Four generator-documentation tests are skipped because their referenced generator skill documents are not part of this repository; runtime tests still run.
- Production Cloudflare frontend build and TypeScript check passed.
- Intelligence Worker dry-run bundle passed.
- Three.js projection tests place all protocol galaxy cores inside narrow portrait and desktop viewports.
- Live public checks before publishing these repairs returned successful Solana and PONS token lookups. The PONS galaxy was empty; Pump Field v0 returned indexed tokens. Those observations do not prove the new ranking code has run in production.
- The cloud browser disables WebGL; it also blocks access to the local preview. Native rendering, touch accuracy, and alien animation on the actual Seeker are not certified by this review.
- This workspace has no Cloudflare login. The previous deployment workflow logs showed a missing CLOUDFLARE_API_TOKEN and a successful skip. Publishing GitHub code is not equivalent to deploying the site.

## Remaining improvement priorities

1. Complete live publication and the phone acceptance checklist in CLOUDFLARE_DEPLOYMENT.md.
2. Monitor PONS indexing/ranking across two scheduled cycles; identify provider authorization or historical-coverage gaps from Worker logs if no tokens qualify.
3. Keep the initial screen focused on choosing a galaxy and token. Move detailed evidence and limitations into the existing investigation tools rather than repeating them in every spoken answer.
4. Expand wallet comparisons and replay only as actual indexed history becomes available. Current configuration is intentionally bounded; it is not full-chain coverage.
5. Treat holder concentration, launchpad attribution, bundled wallets, and smart-money labels as separate evidence problems. Do not infer trader identity, safety, or performance from raw balances alone.

Provider references used for the repairs: https://docs.bitquery.io/docs/blockchain/robinhood/pons-api/ and https://docs.dexscreener.com/api/reference.
