export function nyNow(){return new Date(new Date().toLocaleString('en-US',{timeZone:'America/New_York'}))}export function isCashOpen(date=nyNow()){const d=date.getDay();if(d===0||d===6)return false;const m=date.getHours()*60+date.getMinutes();return m>=570&&m<960}export function sessionLabel(date=nyNow()){return isCashOpen(date)?'Cash session open · research floor live':'Cash session closed · Afterbell open'}export function formatNy(date=new Date()){return new Intl.DateTimeFormat('en-US',{timeZone:'America/New_York',weekday:'short',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(date)+' ET'}