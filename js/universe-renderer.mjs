import { destinationForEntity } from './universe-contracts.mjs';
import { FrameBudgetController, initialUniverseQuality, qualityProfile } from './universe-quality.mjs';
import { HyperspaceTransition } from './universe-transition.mjs';
import { fieldReplayBurst, resolveFieldEffectPosition } from './field-replay-effects.mjs';
const COLORS=Object.freeze({swap:[0.42,0.18,1],transfer:[0.08,0.82,0.96],nft:[1,0.34,0.7],staking:[0.3,1,0.54],program:[1,0.72,0.2],failure:[1,0.2,0.2],unknown:[0.72,0.72,0.8]});
const MODE_STYLE=Object.freeze({explore:[1,1,0],intelligence:[.82,1.08,.08],replay:[1.25,1.18,.2],compare:[.72,1.12,-.08],'what-if':[.9,1.2,.12],sequences:[1.15,1.3,.16],trickster:[1.35,1.12,.28],evidence:[.6,.95,-.12],games:[1.45,1.16,.35]});
const REPLAY_COLOR=Object.freeze({green:[.18,1,.42],red:[1,.12,.16],violet:[.58,.2,1],neutral:[.8,.84,1]});
function capabilitiesFromBrowser(){const canvas=document.createElement('canvas');return{webgl2:Boolean(canvas.getContext('webgl2')),webgpu:Boolean(globalThis.navigator?.gpu),deviceMemory:Number(globalThis.navigator?.deviceMemory??0),saveData:Boolean(globalThis.navigator?.connection?.saveData),reducedMotion:globalThis.matchMedia?.('(prefers-reduced-motion: reduce)').matches??false};}
function createFallback(host,snapshot,onSelect){const list=document.createElement('div');list.className='universe-fallback';list.setAttribute('role','list');const title=document.createElement('p');title.textContent=snapshot.coverageStatement;list.append(title);for(const entity of snapshot.particles.slice(0,250)){const button=document.createElement('button');button.type='button';button.setAttribute('role','listitem');button.textContent=`${entity.kind.toUpperCase()} · ${entity.category} · ${entity.verificationState}`;button.addEventListener('click',()=>onSelect?.(entity,destinationForEntity(entity)));list.append(button);}host.append(list);return()=>list.remove();}
export class UniverseRenderer{
 #host;#snapshot;#three;#renderer;#scene;#camera;#points;#material;#raf=0;#lastFrame=0;#quality;#transition=new HyperspaceTransition();#onSelect;#removeFallback=null;#destroyed=false;#destinationReady=false;#mode='explore';#modeTarget=[1,1,0];#modeCurrent=[1,1,0];#onMode;#onReplay;#onTemporalReplay;#onMarketPhase;#onStoryScene;#replayEffects=[];#phaseEffect=null;#phaseUntil=0;#storyField=null;#cameraTarget={distance:125,yaw:0,pitch:0};
 constructor({host,snapshot,THREE,onSelect,capabilities=capabilitiesFromBrowser()}){if(!(host instanceof Element))throw new TypeError('host element is required');this.#host=host;this.#snapshot=snapshot;this.#three=THREE;this.#onSelect=onSelect;const tier=initialUniverseQuality(capabilities);this.#quality=new FrameBudgetController(tier,capabilities);this.#onMode=e=>this.setMode(e?.detail?.mode);this.#onReplay=e=>this.#queueReplayEffects(e?.detail?.effects);this.#onTemporalReplay=e=>this.#queueReplayEffects(fieldReplayBurst(e?.detail?.events||[]));this.#onMarketPhase=e=>{const effect=e?.detail?.effect;if(!effect)return;this.#phaseEffect=effect;this.#phaseUntil=performance.now()+3600;this.setMode('sequences');};this.#onStoryScene=e=>{const state=e?.detail;if(!state?.camera||!state?.field)return;this.#storyField=state.field;this.#cameraTarget={distance:Number(state.camera.distance)||125,yaw:Number(state.camera.yaw)||0,pitch:Number(state.camera.pitch)||0};this.setMode('trickster');};globalThis.addEventListener('abulls:field-mode',this.#onMode);globalThis.addEventListener('abulls:field-replay-events',this.#onReplay);globalThis.addEventListener('abulls:temporal-replay-events',this.#onTemporalReplay);globalThis.addEventListener('abulls:field-market-phase',this.#onMarketPhase);globalThis.addEventListener('abulls:field-story-scene',this.#onStoryScene);if(!THREE||qualityProfile(tier).renderer==='list'){this.#removeFallback=createFallback(host,snapshot,onSelect);return;}this.#mountThree();}
 #mountThree(){const T=this.#three,profile=this.#quality.profile,canvas=document.createElement('canvas');canvas.className='universe-canvas';canvas.setAttribute('aria-label','Interactive sampled Solana activity field');this.#host.append(canvas);this.#renderer=new T.WebGLRenderer({canvas,antialias:false,alpha:true,powerPreference:'high-performance'});this.#renderer.setPixelRatio(Math.min(globalThis.devicePixelRatio||1,profile.pixelRatio));this.#scene=new T.Scene();this.#camera=new T.PerspectiveCamera(55,1,.1,500);this.#camera.position.set(0,0,125);this.#material=new T.ShaderMaterial({transparent:true,depthWrite:false,vertexColors:true,blending:T.AdditiveBlending,uniforms:{uPixelRatio:{value:this.#renderer.getPixelRatio()},uWarp:{value:0},uIntensity:{value:1}},vertexShader:`attribute float aSize;uniform float uPixelRatio;uniform float uWarp;uniform float uIntensity;varying vec3 vColor;void main(){vColor=color*uIntensity;vec3 warped=position;float depthSign=position.z>=0.0?1.0:-1.0;warped.z+=depthSign*uWarp*(25.0+abs(position.z)*2.2);vec4 mvPosition=modelViewMatrix*vec4(warped,1.0);gl_PointSize=aSize*uPixelRatio*(180.0/max(10.0,-mvPosition.z));gl_Position=projectionMatrix*mvPosition;}`,fragmentShader:`varying vec3 vColor;void main(){vec2 p=gl_PointCoord-vec2(0.5);float d=length(p);float alpha=smoothstep(0.5,0.06,d);gl_FragColor=vec4(vColor,alpha);}`});this.#points=new T.Points(this.#geometryForSnapshot(this.#snapshot),this.#material);this.#scene.add(this.#points);this.#resize();globalThis.addEventListener('resize',this.#resize);canvas.addEventListener('click',this.#pick);this.#lastFrame=performance.now();this.#raf=requestAnimationFrame(this.#frame);}
 #geometryForSnapshot(snapshot){const T=this.#three,visible=snapshot.particles.slice(0,this.#quality.profile.particleLimit),positions=new Float32Array(visible.length*3),colors=new Float32Array(visible.length*3),sizes=new Float32Array(visible.length);visible.forEach((entity,index)=>{positions.set(entity.position,index*3);colors.set(COLORS[entity.category]??COLORS.unknown,index*3);sizes[index]=1.4+entity.magnitudeBand*4;});const geometry=new T.BufferGeometry();geometry.setAttribute('position',new T.BufferAttribute(positions,3));geometry.setAttribute('color',new T.BufferAttribute(colors,3));geometry.setAttribute('aSize',new T.BufferAttribute(sizes,1));geometry.userData.entities=visible;return geometry;}
 #queueReplayEffects(effects){if(!this.#renderer||!Array.isArray(effects))return;const now=performance.now(),particles=this.#points?.geometry?.userData?.entities||this.#snapshot?.particles||[];for(const effect of effects.slice(0,32)){const T=this.#three,color=REPLAY_COLOR[effect.hue]??REPLAY_COLOR.neutral,resolved=resolveFieldEffectPosition(effect,particles),[ax,ay,az]=resolved.position,geometry=new T.BufferGeometry();let object;if(effect.kind==='lightning'){const vertices=[];for(let i=0;i<5;i++){const jitter=(i===0||i===4)?0:(i%2===0?1:-1)*(2+effect.intensity*4);vertices.push(ax+jitter,ay+12-i*6,az+(i%2?2:-2)*effect.intensity);}geometry.setAttribute('position',new T.Float32BufferAttribute(vertices,3));object=new T.Line(geometry,new T.LineBasicMaterial({color:new T.Color(...color),transparent:true,opacity:effect.simulation?.32:.95,blending:T.AdditiveBlending}));}else{geometry.setAttribute('position',new T.Float32BufferAttribute([ax,ay,az],3));object=new T.Points(geometry,new T.PointsMaterial({color:new T.Color(...color),size:5+effect.intensity*12,transparent:true,opacity:effect.simulation?.26:.85,blending:T.AdditiveBlending,depthWrite:false}));}object.userData={born:now,life:450+effect.intensity*950,simulation:Boolean(effect.simulation),matched:resolved.anchor.matched,entityId:resolved.anchor.entityId,entityKind:resolved.anchor.entityKind,eventId:effect.eventId,walletIdentity:resolved.walletIdentity,visualOffset:resolved.offset};this.#scene.add(object);this.#replayEffects.push(object);}}
 #animateReplayEffects(now){for(let i=this.#replayEffects.length-1;i>=0;i--){const object=this.#replayEffects[i],age=now-object.userData.born,progress=age/object.userData.life;if(progress>=1){this.#scene.remove(object);object.geometry?.dispose();object.material?.dispose();this.#replayEffects.splice(i,1);continue;}object.material.opacity=(object.userData.simulation?.32:.95)*(1-progress);object.scale.setScalar(1+progress*(object.isPoints?2.5:.35));if(object.userData.simulation)object.rotation.z=progress*.16;}}
 setMode(mode='explore'){this.#mode=Object.hasOwn(MODE_STYLE,mode)?mode:'explore';this.#modeTarget=[...MODE_STYLE[this.#mode]];if(this.#mode!=='trickster'){this.#storyField=null;this.#cameraTarget={distance:125,yaw:0,pitch:0};}return this.#mode;}
 updateSnapshot(snapshot){if(!snapshot?.particles)throw new TypeError('valid snapshot is required');this.#snapshot=snapshot;if(!this.#renderer){this.#removeFallback?.();this.#removeFallback=createFallback(this.#host,snapshot,this.#onSelect);return;}const previous=this.#points.geometry;this.#points.geometry=this.#geometryForSnapshot(snapshot);previous.dispose();}
 #resize=()=>{if(!this.#renderer)return;const width=Math.max(1,this.#host.clientWidth),height=Math.max(1,this.#host.clientHeight);this.#renderer.setSize(width,height,false);this.#camera.aspect=width/height;this.#camera.updateProjectionMatrix();};
 #pick=event=>{const T=this.#three,rect=this.#renderer.domElement.getBoundingClientRect(),pointer=new T.Vector2(((event.clientX-rect.left)/rect.width)*2-1,-((event.clientY-rect.top)/rect.height)*2+1),raycaster=new T.Raycaster();raycaster.params.Points.threshold=2.5;raycaster.setFromCamera(pointer,this.#camera);const hit=raycaster.intersectObject(this.#points,false)[0];if(!hit)return;const entity=this.#points.geometry.userData.entities[hit.index];if(!entity)return;this.#destinationReady=false;this.#transition.start(entity);this.#onSelect?.(entity,destinationForEntity(entity),this.#transition);};
 #frame=now=>{if(this.#destroyed)return;const elapsed=now-this.#lastFrame;this.#lastFrame=now;const tierBefore=this.#quality.tier,tierAfter=this.#quality.sample(elapsed);if(tierAfter!==tierBefore&&tierAfter==='fallback'){this.destroy();this.#removeFallback=createFallback(this.#host,this.#snapshot,this.#onSelect);return;}const transition=this.#transition.update(now,this.#destinationReady),warp=transition.state==='accelerating'?Math.min(1,Math.max(0,(transition.elapsed-350)/900)):transition.state==='whiteout'?1:0;if(this.#phaseEffect&&now>=this.#phaseUntil)this.#phaseEffect=null;const phase=this.#phaseEffect,story=this.#storyField,target=[this.#modeTarget[0]*(phase?.intensity??story?.intensity??1),this.#modeTarget[1]*(phase?.scale??story?.scale??1),this.#modeTarget[2]+(phase?.rotation??0)];for(let i=0;i<3;i++)this.#modeCurrent[i]+=(target[i]-this.#modeCurrent[i])*.045;this.#material.uniforms.uWarp.value=warp;this.#material.uniforms.uIntensity.value=this.#modeCurrent[0];this.#points.scale.setScalar(this.#modeCurrent[1]);this.#points.rotation.z=this.#modeCurrent[2];this.#points.rotation.y+=Math.min(.003*this.#modeCurrent[0],elapsed*.00008*this.#modeCurrent[0]);const camera=this.#cameraTarget,desiredX=Math.sin(camera.yaw)*camera.distance,desiredY=Math.sin(camera.pitch)*camera.distance,desiredZ=Math.cos(camera.yaw)*camera.distance;this.#camera.position.x+=(desiredX-this.#camera.position.x)*.045;this.#camera.position.y+=(desiredY-this.#camera.position.y)*.045;this.#camera.position.z+=(desiredZ-this.#camera.position.z)*.045;this.#camera.lookAt(0,0,0);this.#animateReplayEffects(now);this.#renderer.render(this.#scene,this.#camera);this.#raf=requestAnimationFrame(this.#frame);};
 cancelTransition(){this.#destinationReady=false;return this.#transition.cancel(performance.now());}markDestinationReady(){this.#destinationReady=true;return this.#transition.update(performance.now(),true);}
 destroy(){this.#destroyed=true;cancelAnimationFrame(this.#raf);globalThis.removeEventListener('resize',this.#resize);globalThis.removeEventListener('abulls:field-mode',this.#onMode);globalThis.removeEventListener('abulls:field-replay-events',this.#onReplay);globalThis.removeEventListener('abulls:temporal-replay-events',this.#onTemporalReplay);globalThis.removeEventListener('abulls:field-market-phase',this.#onMarketPhase);globalThis.removeEventListener('abulls:field-story-scene',this.#onStoryScene);this.#renderer?.domElement.removeEventListener('click',this.#pick);for(const object of this.#replayEffects){this.#scene?.remove(object);object.geometry?.dispose();object.material?.dispose();}this.#replayEffects=[];this.#points?.geometry.dispose();this.#material?.dispose();this.#renderer?.dispose();this.#renderer?.domElement.remove();this.#removeFallback?.();}
}
