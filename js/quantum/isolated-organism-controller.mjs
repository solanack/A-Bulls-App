import { ALIEN_HEAD_TEMPLATE } from './alien-head-template.mjs';

const clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));
const hash01=(v)=>{const n=Math.sin(v*12.9898+78.233)*43758.5453123;return n-Math.floor(n);};

function hostSize(host){const p=host?.parentElement;return{width:Math.max(1,host?.clientWidth||0,p?.clientWidth||0,globalThis.innerWidth||1),height:Math.max(1,host?.clientHeight||0,p?.clientHeight||0,globalThis.innerHeight||1)};}

function eyeMetric(x,y,side){const nx=x/39,ny=y/50,ex=side*0.39,ey=0.20,dx=nx-ex,dy=ny-ey,a=side*0.16,ca=Math.cos(a),sa=Math.sin(a),rx=dx*ca-dy*sa,ry=dx*sa+dy*ca;return(rx/0.37)**2+(ry/0.225)**2;}
function facialVoid(x,y,z){if(z<4.5)return false;if(Math.min(eyeMetric(x,y,-1),eyeMetric(x,y,1))<1.0)return true;const nx=x/39,ny=y/50;if(-0.19<ny&&ny<-0.025&&0.020<Math.abs(nx)&&Math.abs(nx)<0.105)return true;if(Math.abs(nx)<0.225&&ny>-0.465&&ny<-0.365)return true;return false;}
function radialBasis(x,y,z){const l=Math.hypot(x,y,z)||1,n=[x/l,y/l,z/l];let a=[0,1,0];if(Math.abs(n[1])>0.86)a=[1,0,0];let t=[a[1]*n[2]-a[2]*n[1],a[2]*n[0]-a[0]*n[2],a[0]*n[1]-a[1]*n[0]];const tl=Math.hypot(...t)||1;t=t.map(v=>v/tl);const b=[n[1]*t[2]-n[2]*t[1],n[2]*t[0]-n[0]*t[2],n[0]*t[1]-n[1]*t[0]];return{n,t,b};}

function sampleTarget(i,positions,roles,count){let ti=Math.min(count-1,Math.floor(hash01(i*7.917+11.31)*count)),tj=ti*3,sx=positions[tj],sy=positions[tj+1],sz=positions[tj+2],role=roles[ti]||1;for(let k=0;k<40&&facialVoid(sx,sy,sz);k++){ti=Math.min(count-1,Math.floor(hash01(i*7.917+11.31+(k+1)*31.73)*count));tj=ti*3;sx=positions[tj];sy=positions[tj+1];sz=positions[tj+2];role=roles[ti]||1;}sx=-sx;sz=-sz;const h=hash01(i*17.133+3.17),layer=h<0.84?1:h<0.985?2:3,basis=radialBasis(sx,sy,sz),a=hash01(i*1.913+2.7)*Math.PI*2,r=layer===1?0.12+hash01(i*3.731+9.1)*0.48:layer===2?0.25+hash01(i*3.731+9.1)*0.70:0.35+hash01(i*3.731+9.1)*0.90,inward=layer===1?0.08+hash01(i*5.117+4.4)*1.25:layer===2?1.2+hash01(i*5.117+4.4)*4.4:5.0+hash01(i*5.117+4.4)*12.0,ca=Math.cos(a)*r,sa=Math.sin(a)*r;let x=sx+basis.t[0]*ca+basis.b[0]*sa-basis.n[0]*inward,y=sy+basis.t[1]*ca+basis.b[1]*sa-basis.n[1]*inward,z=sz+basis.t[2]*ca+basis.b[2]*sa-basis.n[2]*inward;if(facialVoid(x,y,z)){x-=basis.n[0]*2.2;y-=basis.n[1]*2.2;z-=basis.n[2]*2.2;}return{x,y,z,layer,role,basis};}

export class IsolatedQuantumOrganismController{
  #host;#THREE;#scene;#camera;#renderer;#group;#points;#material;#raf=0;#active=false;#blend=0;#destroyed=false;#onResize;#pointer={active:false,x:0,y:0};#yaw=0;#pitch=0;
  constructor({host,THREE}){this.#host=host;this.#THREE=THREE;if(!host||!THREE)return;const T=THREE;this.#scene=new T.Scene();this.#camera=new T.PerspectiveCamera(52,1,0.1,700);this.#camera.position.set(0,0,118);this.#renderer=new T.WebGLRenderer({alpha:true,antialias:false,powerPreference:'high-performance'});this.#renderer.setClearColor(0x000000,0);this.#renderer.setPixelRatio(Math.min(globalThis.devicePixelRatio||1,1.5));const c=this.#renderer.domElement;c.className='query-organism-canvas';Object.assign(c.style,{position:'absolute',inset:'0',width:'100%',height:'100%',pointerEvents:'none',zIndex:'3',opacity:'0'});host.append(c);this.#group=new T.Group();this.#scene.add(this.#group);this.#build();this.#onResize=()=>this.#resize();globalThis.addEventListener('resize',this.#onResize);c.addEventListener('pointerdown',e=>{if(!this.#active)return;this.#pointer={active:true,x:e.clientX,y:e.clientY};c.setPointerCapture?.(e.pointerId);});c.addEventListener('pointermove',e=>{if(!this.#pointer.active)return;const dx=e.clientX-this.#pointer.x,dy=e.clientY-this.#pointer.y;this.#yaw+=dx*0.005;this.#pitch=clamp(this.#pitch+dy*0.004,-0.85,0.85);this.#pointer.x=e.clientX;this.#pointer.y=e.clientY;});c.addEventListener('pointerup',e=>{this.#pointer.active=false;c.releasePointerCapture?.(e.pointerId);});c.addEventListener('pointercancel',()=>{this.#pointer.active=false;});this.#resize();this.#raf=requestAnimationFrame(this.#frame);}
  #build(){const T=this.#THREE,t=ALIEN_HEAD_TEMPLATE;if(!t?.positions||!t?.roles||!t?.count)return;const palette=[[0.42,0.18,1],[0.08,0.82,0.96],[1,0.34,0.7],[0.3,1,0.54],[1,0.72,0.2],[1,0.2,0.2],[0.72,0.72,0.8]],memory=Number(globalThis.navigator?.deviceMemory||4),count=memory>=8?180000:memory>=4?140000:96000,pos=new Float32Array(count*3),col=new Float32Array(count*3),size=new Float32Array(count),phase=new Float32Array(count),layer=new Float32Array(count);for(let i=0;i<count;i++){const s=sampleTarget(i,t.positions,t.roles,t.count),j=i*3;pos[j]=s.x;pos[j+1]=s.y;pos[j+2]=s.z;const base=palette[Math.floor(hash01(i*9.13)*palette.length)%palette.length],front=clamp((s.basis.n[2]+1)*0.5),side=clamp((s.basis.n[0]+1)*0.5),top=clamp((s.basis.n[1]+1)*0.5),light=clamp(0.22+front*0.48+top*0.24+side*0.10-(s.layer-1)*0.11,0.14,1.05);col[j]=base[0]*light;col[j+1]=base[1]*light;col[j+2]=base[2]*light;size[i]=s.layer===1?0.22+hash01(i*2.37)*0.18:s.layer===2?0.16+hash01(i*2.37)*0.12:0.11+hash01(i*2.37)*0.08;phase[i]=hash01(i*5.91)*Math.PI*2;layer[i]=s.layer;}const g=new T.BufferGeometry();g.setAttribute('position',new T.BufferAttribute(pos,3));g.setAttribute('color',new T.BufferAttribute(col,3));g.setAttribute('aSize',new T.BufferAttribute(size,1));g.setAttribute('aPhase',new T.BufferAttribute(phase,1));g.setAttribute('aLayer',new T.BufferAttribute(layer,1));this.#material=new T.ShaderMaterial({transparent:true,depthWrite:true,depthTest:true,vertexColors:true,blending:T.NormalBlending,uniforms:{uPixelRatio:{value:this.#renderer.getPixelRatio()},uAlpha:{value:0},uTime:{value:0}},vertexShader:`attribute float aSize;attribute float aPhase;attribute float aLayer;uniform float uPixelRatio;uniform float uTime;varying vec3 vColor;varying float vLayer;void main(){vColor=color;vLayer=aLayer;vec3 p=position;float amp=aLayer<1.5?0.05:(aLayer<2.5?0.12:0.28);vec3 dir=normalize(position+vec3(0.0001));p+=dir*sin(uTime*(0.38+0.08*aLayer)+aPhase)*amp;vec4 mv=modelViewMatrix*vec4(p,1.0);gl_PointSize=aSize*uPixelRatio*(104.0/max(12.0,-mv.z));gl_Position=projectionMatrix*mv;}`,fragmentShader:`uniform float uAlpha;varying vec3 vColor;varying float vLayer;void main(){vec2 p=gl_PointCoord-vec2(0.5);float d=length(p);if(d>0.5)discard;float core=smoothstep(0.50,0.06,d);float alpha=core*uAlpha*(vLayer<1.5?1.0:(vLayer<2.5?0.86:0.62));gl_FragColor=vec4(vColor,alpha);}`});this.#points=new T.Points(g,this.#material);this.#points.frustumCulled=false;this.#group.add(this.#points);}
  setActive(active){this.#active=Boolean(active);const c=this.#renderer?.domElement;if(c)c.style.pointerEvents=this.#active?'auto':'none';return this.#active;}
  #resize(){if(!this.#renderer||!this.#camera)return;const{width,height}=hostSize(this.#host);this.#renderer.setSize(width,height,false);this.#camera.aspect=width/height;this.#camera.updateProjectionMatrix();if(this.#material)this.#material.uniforms.uPixelRatio.value=this.#renderer.getPixelRatio();}
  #frame=(now)=>{if(this.#destroyed)return;this.#blend+=( (this.#active?1:0)-this.#blend)*0.085;if(this.#material){this.#material.uniforms.uAlpha.value=clamp(this.#blend*1.02);this.#material.uniforms.uTime.value=now*0.001;}if(this.#group){this.#group.visible=this.#blend>0.002;this.#group.rotation.y=this.#yaw;this.#group.rotation.x=this.#pitch;}const c=this.#renderer?.domElement;if(c)c.style.opacity=String(clamp(this.#blend));if(this.#renderer&&this.#scene&&this.#camera)this.#renderer.render(this.#scene,this.#camera);this.#raf=requestAnimationFrame(this.#frame);};
  destroy(){this.#destroyed=true;cancelAnimationFrame(this.#raf);globalThis.removeEventListener('resize',this.#onResize);this.#points?.geometry?.dispose();this.#material?.dispose();this.#renderer?.dispose();this.#renderer?.domElement?.remove();}
}
